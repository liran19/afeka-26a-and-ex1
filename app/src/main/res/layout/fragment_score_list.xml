<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/fragment_score_list_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#E3F2FD"
    android:padding="8dp">

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/fragment_score_list_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Top 10 Scores"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#1976D2"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/fragment_score_list_recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/fragment_score_list_title"
        android:layout_marginTop="8dp"
        android:paddingBottom="8dp"
        android:clipToPadding="false" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/fragment_score_list_empty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="No scores yet.\nPlay a game to set a record!"
        android:textSize="18sp"
        android:textColor="#666666"
        android:gravity="center"
        android:visibility="gone" />

</RelativeLayout>

